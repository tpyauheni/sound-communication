"""
Module with optional features.

If specific features are available (they are specified in
`/optional_requirements.txt`) then they are used otherwise they are replaced
with empty implementations.
"""
